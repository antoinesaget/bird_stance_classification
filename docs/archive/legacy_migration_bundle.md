# Legacy Migration Bundle

The old `full_migration_bundle/` directory was removed from the active repo surface during the 2026-03-26 cleanup.

It was archived outside the repo at:

- `/Users/antoine/truenas_migration/_archives/bird_stance_classification/2026-03-26/full_migration_bundle`

That archive contains the frozen migration snapshot material:

- app data bundle
- database dumps
- exported inventory CSVs
- runtime/config capture
- migration readme and snapshot metadata

It is historical reference only and should not be treated as part of the live operational path.
