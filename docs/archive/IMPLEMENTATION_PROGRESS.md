# Implementation Progress (vs `PLAN.md`)

Last updated: 2026-02-20

## Update policy
- This file is the running source of truth for implementation state vs `/Users/antoine/bird_leg/PLAN.md`.
- It must be updated on every meaningful implementation step (code, infra, verification, or checkpoint closure).
- Status values:
  - `DONE`: implemented and verified against checkpoint intent.
  - `PARTIAL`: implemented but one or more required verification items still pending.
  - `PENDING`: not yet implemented.

## Snapshot summary
- `DONE`: C0, C1, C2, C3, C4, C5, C6, C7, C8, C9, C10, C11
- `PARTIAL`: none
- `PENDING`: none

## Checkpoint status
| Checkpoint | Status | Evidence | Remaining |
|---|---|---|---|
| C0 Environment contract | DONE | `/Users/antoine/bird_leg/ENVIRONMENT.md`, `/Users/antoine/bird_leg/.env.example`, `/Users/antoine/bird_leg/pyproject.toml`, `/Users/antoine/bird_leg/uv.lock`, `/Users/antoine/bird_leg/Makefile`; `uv run python -V` -> `3.11.14`; compose config validates | None |
| C1 Scaffold + config contract | DONE | `/Users/antoine/bird_leg/src/birdsys/*`, `/Users/antoine/bird_leg/scripts/*`, `/Users/antoine/bird_leg/config/project.example.yaml`, `/Users/antoine/bird_leg/tests/smoke/test_imports.py`; script `--help` checks pass | None |
| C2 Data root + metadata indexing | DONE | Data root initialized at `/Users/antoine/bird_leg/data/birds_project` (container-mapped to `/data/birds_project`); `/Users/antoine/bird_leg/scripts/register_images.py`; `/Users/antoine/bird_leg/data/birds_project/metadata/images.parquet` currently `rows=10037`, `duplicates=0` after adding site `wetransfer_641831510-jpg_2026-02-17_1739` | None |
| C3 Label Studio deployment + annotation config | DONE | Stack up (`postgres`, `label-studio`, `ml-backend`); `/Users/antoine/bird_leg/labelstudio/label_config.xml`; `/Users/antoine/bird_leg/docs/annotator_guide.md`; 50-image import bundle generated at `/Users/antoine/bird_leg/data/birds_project/labelstudio/imports/scolop2_sample50.tasks.json`; strict conditional UI validated in live project with stable prefill+persistence; 50-task export produced at `/Users/antoine/bird_leg/project-4-at-2026-02-19-16-46-a663b8e7.json` | None |
| C4 ML backend baseline + Model A | DONE | `/Users/antoine/bird_leg/services/ml_backend/app/main.py`; `/Users/antoine/bird_leg/services/ml_backend/app/predictors/model_a_yolo.py`; `GET /healthz` -> `model_a_loaded=true`; `/predict` returns bbox + confidence; Label Studio compatibility endpoints exposed (`GET /health`, `POST /setup`, `POST /validate`) and backend validates in Label Studio | None |
| C5 Export normalization + crops | DONE | `/Users/antoine/bird_leg/scripts/export_normalize.py`, `/Users/antoine/bird_leg/scripts/make_crops.py`; latest live export normalized as `/Users/antoine/bird_leg/data/birds_project/labelstudio/normalized/ann_v902/` and crops at `/Users/antoine/bird_leg/data/birds_project/derived/crops/ann_v902/` | None |
| C6 Dataset assembly (DuckDB) | DONE | `/Users/antoine/bird_leg/sql/build_dataset_duckdb.sql`, `/Users/antoine/bird_leg/scripts/build_dataset.py`; latest datasets at `/Users/antoine/bird_leg/data/birds_project/derived/datasets/ds_v907/` (plus previous `ds_v906` for delta reference); split assignment equality check across versions (`same_assignments=True`) and new auto-generated dataset reports (`dataset_report.json`, `dataset_report.md`) | None |
| C7 Model B training pipeline | DONE | `/Users/antoine/bird_leg/scripts/train_attributes.py`, `/Users/antoine/bird_leg/config/train_attributes.yaml`; latest smoke artifacts at `/Users/antoine/bird_leg/data/birds_project/models/attributes/convnextv2s_v005/`; masking unit tests in `/Users/antoine/bird_leg/tests/unit/test_masking_rules.py`; new auto-generated model reports (`report.json`, `report.md`) with deltas vs previous version | None |
| C8 Model C training pipeline | DONE | `/Users/antoine/bird_leg/scripts/train_image_status.py`, `/Users/antoine/bird_leg/config/train_image_status.yaml`; latest smoke artifacts at `/Users/antoine/bird_leg/data/birds_project/models/image_status/status_v004/`; new auto-generated model reports (`report.json`, `report.md`) with deltas vs previous version | None |
| C9 Backend integration A+B+C | DONE | Unified orchestration active in `/Users/antoine/bird_leg/services/ml_backend/app/main.py`; single `/predict` returns bbox + attributes + image_status; 20-image latency benchmark added and run via `/Users/antoine/bird_leg/services/ml_backend/app/benchmark_predict.py` with report at `/Users/antoine/bird_leg/data/birds_project/derived/benchmarks/ml_backend/predict_latency_20260217_113548/latency_report.json`; manual validation confirms attribute prefill and persistence; Model B/C predictors now support checkpoint-backed inference with heuristic fallback (`services/ml_backend/app/predictors/model_b_attributes.py`, `services/ml_backend/app/predictors/model_c_image_status.py`) | None |
| C10 Active learning batch selection | DONE | `/Users/antoine/bird_leg/scripts/infer_batch.py`, `/Users/antoine/bird_leg/scripts/select_active_learning_batch.py`; outputs at `/Users/antoine/bird_leg/data/birds_project/derived/active_learning_infer/run_20260217_115653/`; deterministic selection fixed (stable hash) and verified with repeated seed run | None |
| C11 End-to-end reproducibility gate | DONE | `/Users/antoine/bird_leg/docs/runbook_e2e.md`, `/Users/antoine/bird_leg/docs/reproducibility_checklist.md`, integration tests in `/Users/antoine/bird_leg/tests/integration/`; scripted gate executed for `/Users/antoine/bird_leg/project-4-at-2026-02-20-10-24-00d4bdd1.json` -> `ann_v902` -> `ds_v905` + determinism `ds_v906` -> smoke train (`convnextv2s_v004`, `status_v003`) -> backend contract verify with loaded checkpoints; sign-off record at `/Users/antoine/bird_leg/docs/e2e_signoff_ann_v902.md` | None |

## Verification log (latest)
- `uv run pytest -q` -> `13 passed`
- Reporting enhancement shipped: `build_dataset.py`, `train_attributes.py`, and `train_image_status.py` now emit human-readable and machine-readable reports on every run (`dataset_report.json/.md` for datasets; `report.json/.md` for model trainings) including version-over-version deltas.
- Latest dataset report output: `/Users/antoine/bird_leg/data/birds_project/derived/datasets/ds_v907/dataset_report.json` and `/Users/antoine/bird_leg/data/birds_project/derived/datasets/ds_v907/dataset_report.md`.
- Latest Model B report output: `/Users/antoine/bird_leg/data/birds_project/models/attributes/convnextv2s_v005/report.json` and `/Users/antoine/bird_leg/data/birds_project/models/attributes/convnextv2s_v005/report.md` (comparison vs `convnextv2s_v004`).
- Latest Model C report output: `/Users/antoine/bird_leg/data/birds_project/models/image_status/status_v004/report.json` and `/Users/antoine/bird_leg/data/birds_project/models/image_status/status_v004/report.md` (comparison vs `status_v003`).
- E2E export input (latest): `/Users/antoine/bird_leg/project-4-at-2026-02-20-10-24-00d4bdd1.json` (`tasks=240`, `cancelled_first_annotations=172`).
- Normalization + crops (latest): `ann_v902` with `images_rows=240`, `birds_rows=848`; crops processed `848`.
- Dataset (latest): `ds_v905` (`train=594`, `val=203`, `test=51`); determinism rebuild `ds_v906` (`same_assignments=True`).
- Training (latest): Model B `convnextv2s_v004` and Model C `status_v003` smoke artifacts written (offline `--no-pretrained` mode).
- Backend gated verify with latest checkpoints (`convnextv2s_v004`, `status_v003`) via FastAPI TestClient: `/health` shows B/C loaded and `/predict` returns valid `predictions/results` envelopes for both absolute path and Label Studio local-files inputs.
- Live backend health check (host mode): `GET http://127.0.0.1:9091/health` -> `status=UP`, `model_a_loaded=true`, `model_a_device=mps`.
- Backend checkpoint-runtime capability added: `/health` now exposes `model_b_loaded` and `model_c_loaded`; when `MODEL_B_CHECKPOINT` / `MODEL_C_CHECKPOINT` are unset, backend remains on heuristic fallback (`model_b_loaded=false`, `model_c_loaded=false`).
- Checkpoint-backed backend live validation (2026-02-20): backend restarted with `MODEL_B_CHECKPOINT=/Users/antoine/bird_leg/data/birds_project/models/attributes/convnextv2s_v003/checkpoint.pt` and `MODEL_C_CHECKPOINT=/Users/antoine/bird_leg/data/birds_project/models/image_status/status_v002/checkpoint.pt`; `/health` reports `model_b_loaded=true`, `model_c_loaded=true`.
- Live `/predict` validation with checkpoints loaded:
  - Absolute path sample (`id=9201`): keys `predictions/results`, `bbox=2`, `readability=2`, `specie=2`, `behavior=2`, `substrate=2`, `legs=0`, `image_status=1`.
  - Label Studio local-files sample (`id=9202`): keys `predictions/results`, `bbox=19`, `readability=19`, `specie=19`, `behavior=19`, `substrate=19`, `legs=0`, `image_status=1`.
- Label Studio log check after checkpoint activation: no `ML backend returns an incorrect response` errors in recent `birds-label-studio` logs.
- Generated Label Studio import bundle for site `wetransfer_641831510-jpg_2026-02-17_1739` (190 images): `/Users/antoine/bird_leg/data/birds_project/labelstudio/imports/wetransfer_641831510_jpg_2026_02_17_1739_all.tasks.json` and `/Users/antoine/bird_leg/data/birds_project/labelstudio/imports/wetransfer_641831510_jpg_2026_02_17_1739_all.files.csv`.
- Live `/predict` contract check with absolute image path: top-level keys `predictions` + `results`; returned bbox + per-bird core attributes + image_status.
- Live `/predict` check with Label Studio local-files URL (`/data/local-files/?d=birds_project/...`): returned `bbox=19`, `readability=19`, `specie=19`, `behavior=17`, `substrate=17`, `legs=6`, `image_status=1` for sample task.
- Label Studio server log sanity check (`birds-label-studio`, tail 300): no `ML backend returns an incorrect response` errors after response-contract fix.
- Live annotation export processed: `/Users/antoine/bird_leg/project-4-at-2026-02-19-16-46-a663b8e7.json` -> normalized as `ann_v901` with `tasks=50`, `images_rows=50`, `birds_rows=169`.
- Crop generation: `/Users/antoine/bird_leg/data/birds_project/derived/crops/ann_v901/` with `processed=169`.
- Dataset build: `ds_v902` from `ann_v901` with rows `train=106`, `val=45`, `test=18`; manifest at `/Users/antoine/bird_leg/data/birds_project/derived/datasets/ds_v902/manifest.json`.
- Determinism check: rebuild to `ds_v903` yields identical split assignment vs `ds_v902` (`same_assignments=True`).
- Model B smoke training on new data: `/Users/antoine/bird_leg/data/birds_project/models/attributes/convnextv2s_v003/` (`checkpoint.pt`, `metrics.json`, `legs_confusion_matrix.csv`) using offline flag `--no-pretrained`.
- Model C smoke training on new data: `/Users/antoine/bird_leg/data/birds_project/models/image_status/status_v002/` (`checkpoint.pt`, `metrics.json`) using offline flag `--no-pretrained`.
- Backend smoke endpoint check was not rerun at this stage because no backend process was listening on host ports `9090` or `9091` during execution.
- `docker compose ... ps` -> all services up
- `ml-backend /healthz` -> healthy and `model_a_loaded=true`
- `ml-backend /predict` smoke -> success on sample image (`has_error=False`)
- Latency smoke (20 images): mean `2853.7049 ms`, p95 `3430.6038 ms`, max `4263.5895 ms`
- Label Studio manual-login unblock: reset password for existing `admin@local` user to `admin` (Django auth check passed).
- Label Studio password reset (follow-up): reset both `admin@local` and `antoinesaget19@gmail.com` to `admin` (Django auth checks passed for both).
- Label Studio image loading unblock: created `LocalFilesImportStorage` for project `4` with path `/data/birds_project/raw_images/scolop2`, enabling `/data/local-files/?d=...` task URLs.
- Label Studio ML backend validation unblock: added `GET /health` compatibility endpoint (in addition to `/healthz`) and rebuilt `ml-backend`; both return HTTP 200.
- Label Studio ML backend validation unblock (follow-up): added `POST /setup` and `POST /validate`; verified from `label-studio` container to `http://ml-backend:9090/setup` -> HTTP 200 JSON response.
- Labeling config validation unblock: `activity`, `support`, `legs`, `resting_back` set to `required="false"` to avoid hidden-field required errors when bird is unreadable.
- Label Studio prediction path unblock: fixed local-files query path resolution in `/Users/antoine/bird_leg/services/ml_backend/app/main.py` so `?d=birds_project/...` resolves to `/data/birds_project/...` inside `ml-backend`; container-level `/predict` check now returns actual bbox+attributes instead of `FileNotFoundError`.
- Prediction serialization compatibility update: per-region choice predictions now include explicit `id` + `parentID` (in `/Users/antoine/bird_leg/services/ml_backend/app/serializers.py`) to improve Label Studio UI linkage for attribute suggestions.
- Host ML backend mode validated for Apple Silicon path: started host backend on `:9091`, verified from Label Studio container via `http://host.docker.internal:9091/health` returning `"model_a_device":"mps"`; local-files path mapping extended so host mode resolves `/data/local-files/?d=birds_project/...` into `${BIRDS_DATA_ROOT}` correctly.
- Label config compatibility adjustment for suggestion acceptance: switched `activity`, `support`, `legs`, and `resting_back` from chained `visibleWhen=choice-selected` dependencies to `visibleWhen=region-selected` (still `required=false`) in `/Users/antoine/bird_leg/labelstudio/label_config.xml` so Label Studio keeps these predicted fields when converting model suggestions into annotations.
- Label config persistence hardening: removed conditional visibility for `activity`, `support`, `legs`, and `resting_back` so these per-region fields are always rendered and saved reliably (`required=false` retained; masking logic remains downstream in normalization/training).
- Project runtime setting alignment (Project `id=4`): set `model_version=birdsys-backend-v0.1`, `show_collab_predictions=false`, and `evaluate_predictions_automatically=true` to reduce model-tab/annotation-tab confusion and ensure predictions are generated in the annotation flow.
- Manual validation follow-up (2026-02-17): observed user confusion between prediction tab and annotation tab; DB verification confirmed full per-bird attributes are present in prediction payloads and persisted annotation records on edited tasks. Applied active-project config directly from repository `label_config.xml` and removed predictions for already-annotated tasks in project `4` to avoid apparent post-save reversion in UI.
- Prediction format compatibility hardening: updated `/Users/antoine/bird_leg/services/ml_backend/app/serializers.py` to emit per-region choice results with region `id` and bbox geometry (`x/y/width/height/rotation`) in addition to `parentID`, matching Label Studio annotation-style payloads more closely for stable prefill rendering.
- Normalization parser compatibility hardening: `/Users/antoine/bird_leg/scripts/export_normalize.py` now supports per-region choices linked by region `id` (newer LS serialization) as well as `parentID`; covered by new test `test_export_normalize_supports_region_id_linked_choices` in `/Users/antoine/bird_leg/tests/integration/test_export_normalize.py`.
- Project runtime stabilization for manual validation: restarted `label-studio`, and set project `4` flags to stop automatic prediction recreation (`show_collab_predictions=false`, `evaluate_predictions_automatically=false`) so deleted predictions no longer reappear automatically.
- Prediction overwrite hardening: `/Users/antoine/bird_leg/services/ml_backend/app/main.py` `/predict` response now returns only top-level `predictions` (removed duplicate top-level `results`) to avoid ambiguous Label Studio state handling during update/submit cycles.
- Validation docs hardened: `/Users/antoine/bird_leg/docs/annotator_guide.md` and `/Users/antoine/bird_leg/docs/runbook_e2e.md` now explicitly require disabling `Interactive preannotations` and keeping `Auto-Accept Suggestions` off during persistence QA.
- Manual validation update (2026-02-17): `manual_values_persist_after_update=PASS`, `deleted_predictions_stay_deleted=PASS` after disabling interactive preannotations. At this step, `single_vs_label_all_consistent` was still failing; this was later resolved by enabling "Use predictions to prelabel tasks".
- Labeling UI efficiency update: redesigned `/Users/antoine/bird_leg/labelstudio/label_config.xml` into a 2-column layout (image left, controls right) to eliminate repeated vertical scrolling; added refresh/apply instructions to `/Users/antoine/bird_leg/docs/annotator_guide.md`.
- Manual validation update (2026-02-17, follow-up): with `Interactive preannotations=OFF` and `Use predictions to prelabel tasks=ON`, workflow is stable (`manual_values_persist_after_update=PASS`, `deleted_predictions_stay_deleted=PASS`, `single_vs_label_all_consistent=PASS`).
- UI validation update (2026-02-17, follow-up): `two_column_layout_visible=PASS`, `annotation_speed_improved=PASS`.
- Strict schema migration (2026-02-17, latest): migrated annotation/prediction pipeline from (`activity`, `support`, `resting_back`) to (`specie`, `behavior`, `substrate`) with new readability classes (`readable`, `occluded`, `unreadable`) and behavior classes (`flying`, `foraging`, `resting`, `backresting`, `preening`, `display`, `unsure`).
- Strict conditional Mode B restored (2026-02-17, latest): `/Users/antoine/bird_leg/labelstudio/label_config.xml` now uses strict nested `visibleWhen` gates for new schema; added three tryout variants under `/Users/antoine/bird_leg/labelstudio/variants/`.
- Variant fix (2026-02-17, latest): corrected `/Users/antoine/bird_leg/labelstudio/variants/label_config_b2_strict_collapse.xml` to keep `Collapse` semantics valid (only `Panel` children), resolving Label Studio `ConfigurationError: Not expecting tag: view`.
- Variant UX refinement (2026-02-17, latest): compacted `/Users/antoine/bird_leg/labelstudio/variants/label_config_b2_strict_collapse.xml` with per-choice subtitles and reduced vertical spacing (smaller headers, tighter section margins, narrower gaps) for faster annotation scan speed.
- Variant UX refinement (2026-02-17, follow-up): simplified `/Users/antoine/bird_leg/labelstudio/variants/label_config_b2_strict_collapse.xml` to remove fragile top-level `region-selected` wrapper (panels now reliably render), while retaining strict conditional gating for Context/Legs and compact right-pane sizing.
- Variant naming tweak (2026-02-17, latest): updated `/Users/antoine/bird_leg/labelstudio/variants/label_config_b1_strict_nested.xml` to use explicit per-choice titles requested by user: `Readability`, `Specie`, `Behavior`, `Substrate`, `Stance`.
- Variant hotkey/order tweak (2026-02-17, latest): updated `/Users/antoine/bird_leg/labelstudio/variants/label_config_b1_strict_nested.xml` per requested keymap and order: image_status (`a`,`q`), readability (`z`,`s`,`x`), specie (`e`,`d`,`c`), behavior reordered to `resting,foraging,flying,backresting,preening,display,unsure` with (`r`,`f`,`v`) on first three only, substrate keys (`t`,`g`,`b`, none), and stance keys (`y`,`h`,`n`, none) with added `sitting` option.
- Schema compatibility update (2026-02-17, latest): extended stance/legs domain to include `sitting` in normalization/training/inference utilities (`export_normalize`, `train_attributes`, `birdsys.training.attributes`, `birdsys.training.models`, `infer_batch`, `select_active_learning_batch`) so new annotations remain end-to-end valid.
- Strict-prelabel compatibility fix (2026-02-19, latest): updated `/Users/antoine/bird_leg/services/ml_backend/app/serializers.py` to emit only conditionally valid fields for strict LS config (`readability/specie` always, `behavior/substrate` only when visible, `legs` only for `resting|backresting` + `ground|water`), preventing Label Studio from dropping full prediction bundles due to hidden-field mismatches.
- Label Studio ML contract fix (2026-02-19, latest): identified runtime contract mismatch in running Label Studio container code (`/label-studio/label_studio/ml/models.py` expects top-level `results` list). Updated `/Users/antoine/bird_leg/services/ml_backend/app/main.py` to return both `results` and `predictions` via `/Users/antoine/bird_leg/services/ml_backend/app/response_contract.py`, added request/response-shape logs, and updated `/Users/antoine/bird_leg/services/ml_backend/app/benchmark_predict.py` to parse either schema.
- Contract tests added (2026-02-19, latest): expanded `/Users/antoine/bird_leg/tests/integration/test_backend_contract.py` with explicit response-envelope tests (`results` + `predictions`) and extraction fallback behavior.
- Manual validation (2026-02-19, latest): user confirmed backend payload contains both `results` and `predictions` (`1/1`), and Label Studio now shows bbox + attribute prefill again.
- Labeling UX bugfix (2026-02-19, latest): set `legs` to `required="false"` in `/Users/antoine/bird_leg/labelstudio/label_config.xml` and `/Users/antoine/bird_leg/labelstudio/variants/label_config_b1_strict_nested.xml` to prevent false "legs is required" validation on non-resting birds due to hidden-field required checks in Label Studio.
- Debuggability hardening (2026-02-17, latest): added task-level and per-detection prediction logs in `/Users/antoine/bird_leg/services/ml_backend/app/main.py`; added deterministic normalization `mask_stats` summary output in `/Users/antoine/bird_leg/scripts/export_normalize.py`.
